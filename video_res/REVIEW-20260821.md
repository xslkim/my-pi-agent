# video_res 课程资料 Review

> 审查日期：2026-08-21 · 范围：`video_res/` 全部 5 课（meta.md + script.md）、README、assets 工具与素材
> 审查基准：`D:/AutoVideo/docs/AUTHORING.md` 规范 + 与仓库实际代码/文档逐项核对

## 总评

整体质量高。脚本结构完全符合 AUTHORING 规范（分块、指令、section 顺序、字幕长度、安全区、lineTimings 用法全部合规），叙事线清楚，"真机实录 + html 截图为主"的模式选择正确。9 段视频素材全部存在、时长与文档一致。

但发现 **2 处硬数据错误**、**3 处文档过时/不一致**、**2 处明显的视觉效果风险**，以及若干小瑕疵。按下面分级列出。

---

## A. 数据错误（建议必改）

### A1. `bash.ts` 行数过时：L3-B13 写 89 行，实际 129 行

`lesson3-coding/script.md` B13 的对比卡片写「我们 · bash.ts / 89 / 超时 · 进程组 · 截断」。
实际 `src/tools/bash.ts` 为 **129 行**（T23 死锁修复加 kill $(jobs -p) 清场后变长了）。
B13 旁白虽没念数字，但卡片大字「89」会出现在画面上，且这一课的主题恰是"诚实给出行数差"。

### A2. L1-B18「七十八行」与同视频 B04 的「149 行」自相矛盾

`lesson1-talk/script.md` B04 卡片：我们 · llm.ts **149** 行（与实际 `src/llm.ts` = 149 行一致 ✓）。
但 B18 旁白念的是「我们只服务一个端点，**七十八行**」。
149 行的文件、51 行的 parseSSE，任何口径都得不到 78，观众听完 B04 再听 B18 会直接发现矛盾。建议 B18 改为 149。

---

## B. 文档过时 / 不一致

### B1. `assets/README.md` 的 L1 块号映射表全部过时

课程总览并入第 1 课后，L1 块号整体后移，但映射表没更新：

| 素材 | 表中写的 | 实际块 |
|---|---|---|
| curl-sse.mp4 | L1-B02 | **L1-B09** |
| talk-demo.mp4 | L1-B10 | **L1-B17** |
| pi-scroll.mp4 | L1-B11 | **L1-B18** |

L2–L5 的映射核对无误。如果后续要 `wire-videos.mjs` 或重录素材，按旧表找块会错位。

### B2. `video_res/README.md`「animation（每课 2–3 块）」不准确

实际 animation 块数：L1 = 4（B02/B08/B12/B13）、L2 = 4（B01/B04/B06/B09）、L3 = 3、L4 = 4（B01/B03/B08/B10）、L5 = 3。
三课超出"2–3 块"的描述。这关系到构建时 AI 生成组件的调用量预估，建议改为「每课 3–4 块」。

### B3. README 目录表与 meta.md 标题不完全一致（轻微）

README 表：`第 1 课 · 让模型说话（含课程总览）`；meta.md：`（课程总览 + 手写 SSE 客户端）`。
L2 也有类似的简称/全称差异（「（loop）」vs「（tool calling 与 agent loop）」）。建议统一以 meta.md 为准。

---

## C. 视觉效果风险（video 模式时长失配）

按旁白字数 ~4.5 字/秒 + 行间 200ms 估算块时长，对比素材实际时长：

| 块 | 素材 | 旁白约 | 视频实长 | 效果 |
|---|---|---|---|---|
| L1-B18 | pi-scroll.mp4 | ~18s | 3.4s | **循环 ~5.4 次** |
| L2-B12 | pi-loop.mp4 | ~21s | 2.5s | **循环 ~8.3 次** |
| L1-B09 | curl-sse.mp4 | ~20s | 15.3s | 循环 1.3 次 |
| L1-B17 | talk-demo.mp4 | ~21s | 16.0s | 循环 1.3 次 |
| L2-B11 | calc-tools.mp4 | ~19s | 15.2s | 循环 1.3 次 |
| L3-B12 | hellojs-demo.mp4 | ~23s | 17.8s | 循环 1.3 次 |
| L4-B11 | session-resume.mp4 | ~22s | 15.4s | 循环 1.4 次 |
| L5-B06 / B10 | run1/run2 | ~15s | 23.8s / 17.1s | 截断，OK |

**两个 pi 源码滚动视频是硬伤**：循环意味着滚动到 560 行后跳回第 60 行重来，5–8 次循环肉眼可见地穿帮。建议用 `render.mjs --pi` 重录：扩大行区间或放慢速度，把时长做到 ~18–20s，正好覆盖旁白。
其余 4 段终端实录循环 1.3 次问题不大（命令重新打一遍可见但可接受）；在意的话同样可以补录。

---

## D. 口径问题（需作者确认）

### D1. 课时说法与视频时长矛盾

- L1-B06 旁白：「最后**十分钟** 对照 pi」
- L3-B02 旁白：「裸实现，**二十分钟**写完……接下来的**四十分钟**，专门用来打碎这份兴奋」

README 标注每课视频仅 ~4.5–7 min。如果视频本身就是课程载体，这些"课时"说法不成立；如果是面授/直播课的配套视频则没问题。二选一：改旁白措辞，或接受现状。

### D2. L3-B12 旁白混入英文短语

「**watching it work** 的这一刻」——中文 TTS 读英文短语的发音不可控，建议改为中文（如「亲眼看它工作的这一刻」）。

---

## E. 小瑕疵（可选修复）

1. **L1-B10 的 JSON 示例有尾逗号**：delta 对象 `"reasoning_content": "想想",` 后直接 `}`。一门讲"协议要看真实字节"的课，示例 JSON 最好也严谨。
2. **L4-B07 注释与系数不完全一致**：注释写「实测值 + 20% 余量」，cjk 0.7/0.58 ≈ +21% 成立；但 `rest / 3.5` 相对实测 3.7 字符/token 只有约 +6% 余量。要么调 3.5 → 3.1，要么把注释改为「cjk +20%、代码 +6%」。
3. **L4-B06 占比条数值对不上**：三条条宽 68%/39%/5%（合计 >100%），而标注的 token 数按 budget 24,000 算应为 22.5%/12.9%/0.75%。示意图配精确数字容易误导，建议条宽改按 budget 比例，或去掉百分比含义的暗示。
4. **`assets/README.md` 的 curl 示例带明文 token** `sk-local-qwen36`。上一次封板审查刚移除了 cli --help 里的硬编码内网端点，这里的 key 字面量建议同样改为 `$LLM_API_KEY`。
5. **L3-B10 HTML 暴露真实用户名** `C:\Users\xsl\...`。本地课件无妨，公开发布前建议替换。
6. **遗留文件**：`video_res/start.mp4`（14.5s）、`end.png`、`testsuccess.png` 在所有 md/mjs 中零引用，疑似残留，建议确认后删除。
7. **git 状态**：`00-course-overview/` 的删除（D 未暂存）和 README、lesson1 的修改尚未提交；`graphify-out/` 未跟踪。记得一并收尾。

---

## 已核对无误的项（供放心的清单）

- **规范符合性**：块结构 / section 顺序 / 指令取值全部合法；html 块均为完整 HTML 文档（1920×1080）；animation 块均声明 lineTimings 驱动、背景填满、避让字幕区；旁白行长 0 条超过 50 字上限（脚本全量扫描）；未使用 image API 模式（与 README 声明一致）；slug 全部小写连字符。
- **块数与 README 表一致**：19/12/13/11/12 ✓；9 段视频素材全部存在、ffprobe 时长与 assets/README 表一致 ✓。
- **硬数据核实**（与仓库实况逐项比对）：
  - pi `openai-completions.ts` 1577 行 ✓、`agent-loop.ts` 796 行 ✓、`edit.ts + edit-diff.ts` 627 行 ✓、`env/nodejs.ts` 695 行 ✓
  - 我方 `llm.ts` 149 ✓、`loop.ts` 90 ✓、`edit.ts` 60 ✓、全项目 1106 行（「一千一百行」）✓
  - `node --test`：110 tests / **108 pass** / 2 skipped，与 L1-B05 的「✔ pass 108」一致 ✓；Node v25.2.1 ✓
  - 「预判清单七项缺口」有出处（docs/lessons/05-delivery.md）✓
- **voiceRef 假设**：`../../B00.wav` 的复制约定 README 已自述，属已知事项，不算问题。

## 建议的处理顺序

1. 改 A1、A2 两处数字（内容错误，进视频就改不了了）
2. 重录两段 pi 源码滚动（C 项硬伤）
3. 顺手更新 assets/README 映射表和 video_res/README 的块数/标题（B 项）
4. D1 口径想清楚再定；E 项看心情
