# L5 · run1 裸跑记录（2026-08-17）

> 命令：`node src/cli.ts --cwd demo/login-app -s l5-run1 --max-steps 30 "$(cat acceptance/task-prompt.md)"`
> 环境：Windows / Git Bash / Node v25.2.1 / qwen3.8-27b（局域网）
> 全程录屏。中途发生一次工具层死锁（见下），人工救援一次，其余零干预。

## 结果：一次裸跑即通过锁定验收（10/10）

`node --test acceptance/login-app.smoke.ts` 全绿，`verify-lock.ts` 通过（考卷未被改动）。
课程预判「第一次大概率做不完」没有发生——这个结果本身要如实记录：预判清单里的大多数缺口
（看不见目录、找不到符号、步数不够、上下文吃满）在这次任务里都没有出现。

## 运行数据

| 指标 | 值 |
|---|---|
| 步数 | 11 / 30（未触及上限） |
| 工具调用 | 17 次：bash 7 · write 5 · edit 4 · read 1 |
| 会话规模 | 30 条消息，估算 ~11.6K token（预算 24K，未触发裁剪） |
| 墙钟时间 | 约 26 分钟（23:18–23:44，按产物时间戳估算） |
| 产物 | server.ts(220 行) · public/login.html · public/app.js · package.json(无 dependencies) · README.md |

## 失败记录表（本课最重要的产出）

| # | 现象 | 根因 | 对策（改进项） | 状态 |
|---|---|---|---|---|
| 1 | agent 用 `node server.ts > server.log 2>&1 &` 后台起服务自测，bash 工具 15s 超时后杀掉 bash 仍未返回，**调用永久挂死**；人工杀掉 3100 端口进程后才恢复 | T13 bash 工具等待 `close`（全部 stdio 关闭）而非 `exit`（进程退出）；后台守护进程继承管道时，进程死了管道也关不上 | **T23（最高优先级）**：改在 `exit` 时返回并主动 destroy 流；含 `&` 的后台命令给出明确指引 | 待修 |

恢复后的自愈行为值得记录：agent 收到超时错误后，下一轮主动先 `pkill -f "node server.ts"`
清场，再写「起服务 → curl 断言 → 打印状态码」的一次性脚本自测，13 项全过——错误信息驱动的
自我纠正是真实发生的。

## 预判清单核对（以实际为准）

| 预判缺口 | 是否发生 | 说明 |
|---|---|---|
| 没有 `ls`，反复猜文件名 | **否** | 空目录起步，全部文件由它自己 write，目录结构在上下文里 |
| 没有 `grep`，Windows 上行为不一致 | **否** | 未出现符号搜索需求 |
| 步数不够 | **否** | 11/30 |
| 上下文吃满 | **否** | ~11.6K / 24K 预算 |
| `edit` 报错后原样重试 | **否** | 4 次 edit 一次成功，其中一次修复 SQL（`SELECT 1 AS exists` 语法错），报错信息有效 |
| 不验证产出 | **否** | 自测了 13 项，含考卷没要求的边界（路径穿越、伪造 cookie、空字段 400） |
| 危险命令 | 基本否 | `pkill node server.ts` 属合理清场，无 `rm -rf` 类操作 |

## 对 T22/T23 的调整建议（依 T21 纪律：由证据驱动）

- **T22（ls/grep，110 行）：不做。** run1 没有出现任何指向它的证据。空目录施工场景里它的价值
  显示不出来；留给延伸练习（换一个「在现有代码库里改功能」的任务时它才会成为瓶颈）。
- **T23（90 行）：缩小为失败表 #1 的 bash 工具修复。** 这是 run1 唯一真实出现、且必须人工救援
  才能恢复的问题。其余预判加固项全部无证据，不做。
- 教学叙事顺带更新：lesson 05 的「预期失败」在本班变成了「工具层死锁」——仍然是失败记录
  驱动加固，课程核心方法不变。
